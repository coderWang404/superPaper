# This file was auto-generated, do not edit it directly.
# Instead run bin/update_build_scripts from
# https://github.com/superpaper/internal/

services:
  test_unit:
    image: ci/$PROJECT_NAME:$BRANCH_NAME-$BUILD_NUMBER
    user: node
    volumes:
      - ./reports:/superpaper/services/notifications/reports
      - ../../bin/shared/wait_for_it:/superpaper/bin/shared/wait_for_it
      - ../../tsconfig.backend.json:/superpaper/tsconfig.backend.json
    entrypoint: /superpaper/bin/shared/wait_for_it mongo:27017 --timeout=60 --
    command: yarn run test:unit:_run
    environment:
      CI:
      MONGO_CONNECTION_STRING: mongodb://mongo/test-superpaper
      NODE_ENV: test
      NODE_OPTIONS: "--unhandled-rejections=strict"
      VITEST_NO_CACHE: true
    depends_on:
      mongo:
        condition: service_started

  test_acceptance:
    build: .
    image: ci/$PROJECT_NAME:$BRANCH_NAME-$BUILD_NUMBER
    environment:
      CI:
      RETRIES:
      MONGO_HOST: mongo
      POSTGRES_HOST: postgres
      MOCHA_GREP: ${MOCHA_GREP}
      NODE_ENV: test
      NODE_OPTIONS: "--unhandled-rejections=strict"
    volumes:
      - ./reports:/superpaper/services/notifications/reports
      - ../../bin/shared/wait_for_it:/superpaper/bin/shared/wait_for_it
      - ../../tsconfig.backend.json:/superpaper/tsconfig.backend.json
    depends_on:
      mongo:
        condition: service_started
    user: node
    entrypoint: /superpaper/bin/shared/wait_for_it mongo:27017 --timeout=60 --
    command: yarn run test:acceptance

  tar:
    build: .
    image: ci/$PROJECT_NAME:$BRANCH_NAME-$BUILD_NUMBER
    volumes:
      - ./:/tmp/build/
    command: tar -czf /tmp/build/build.tar.gz --exclude=build.tar.gz --exclude-vcs .
    user: root

  mongo:
    image: mongo:8.0.11
    command: --replSet superpaper --notablescan
    volumes:
      - ../../bin/shared/mongodb-init-replica-set.js:/docker-entrypoint-initdb.d/mongodb-init-replica-set.js
      - ../../bin/shared/mongodb-docker-entrypoint-wait.sh:/mongodb-docker-entrypoint-wait.sh
    entrypoint: /mongodb-docker-entrypoint-wait.sh
    environment:
      MONGO_INITDB_DATABASE: sharelatex
    extra_hosts:
      # Required when using the automatic database setup for initializing the
      # replica set. This override is not needed when running the setup after
      # starting up mongo.
      - mongo:127.0.0.1
